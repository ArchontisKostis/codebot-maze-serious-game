/**
 * Statement
 *
 * Marker interface for all AST nodes that represent a program statement.
 * Implementations are pure data — no execution logic lives here.
 *
 * Grammar:
 *   statement ::= MoveStatement | RepeatStatement
 */
public interface Statement { }
